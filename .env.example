# Copy to .env.local for local dev. Everything NEXT_PUBLIC_* is shipped to the
# browser -- never put a secret here.
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_COGNITO_DOMAIN=
NEXT_PUBLIC_COGNITO_CLIENT_ID=
